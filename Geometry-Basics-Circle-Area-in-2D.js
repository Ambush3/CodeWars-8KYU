// Take in a circle object and return the area of the circle

circleArea = (circle) => Math.PI * circle.radius * circle.radius;
// circle.radius is the radius of the circle
// math.pi is the pi in the math library